import styled from 'styled-components';

export default function ProductListPage() {
	return (
		<StyledContainer>
			<StyledWrapper>상품리스트 페이지</StyledWrapper>
		</StyledContainer>
	);
}

const StyledContainer = styled.section``;

const StyledWrapper = styled.div``;
